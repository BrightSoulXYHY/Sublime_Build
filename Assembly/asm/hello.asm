DATAS  SEGMENT
     STRING  DB  13,10,'Hello World!',13,10,'$'
DATAS  ENDS

STACK SEGMENT STACK
STAPN DB 20 DUP(?)
TOP EQU LENGTH STAPN
STACK ENDS

CODES  SEGMENT
     ASSUME    CS:CODES,DS:DATAS
START:
    MOV AX,DATAS
    MOV DS,AX
    LEA DX,STRING
    MOV AH,9
    INT 21H
    MOV AL,'5'
    PUSH AX
    POP AX
    MOV DL,AL
    MOV AH,02H

    SUB SP,2
    POP AX
    MOV DL,AL
    MOV AH,02H
    INT 21H

    MOV AH,4CH
    INT 21H
CODES  ENDS
    END   START


